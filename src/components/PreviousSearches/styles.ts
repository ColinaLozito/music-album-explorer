import { StyleSheet } from "react-native";

export const previousSearchesStyles = StyleSheet.create({
  container: {
    marginTop: 24,
    marginBottom: 8,
    borderRadius: 8,
    backgroundColor: "#fff",
    overflow: "hidden",
  },
});
